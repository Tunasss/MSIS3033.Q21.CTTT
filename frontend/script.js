// Frontend JavaScript for Expense Tracker

document.addEventListener('DOMContentLoaded', function() {
    const app = document.getElementById('app');
    
    // Initialize the application
    function init() {
        app.innerHTML = '<p>Expense Tracker Application - Frontend Ready</p>';
    }
    
    init();
});
